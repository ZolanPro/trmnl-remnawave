{% assign active_nodes = 0 %}
{% if IDX_2.response %}
  {% for node in IDX_2.response %}
    {% if node.isConnected and node.isDisabled == false %}
      {% assign active_nodes = active_nodes | plus: 1 %}
    {% endif %}
  {% endfor %}
{% endif %}
